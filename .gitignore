# Intermediate files generated by GitHub Actions
# These are created only during deployment and shouldn't be committed
*/icons.json
*/index.html
search-index.json
metadata.json

# Keep template folder as it contains the master template
!template/